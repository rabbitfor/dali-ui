CMAKE_MINIMUM_REQUIRED(VERSION 3.8.2)

PROJECT(dali-ui-manual-tests)

SET(CMAKE_CXX_STANDARD 17)
SET(CMAKE_C_STANDARD 99)

# Auto-discover test app subdirectories (one per module, e.g. dali-ui-foundation)
FILE(GLOB CHILDREN RELATIVE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/*)
FOREACH(CHILD ${CHILDREN})
  IF(IS_DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR}/${CHILD}
     AND EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/${CHILD}/CMakeLists.txt)
    MESSAGE(STATUS "Adding manual test app: ${CHILD}")
    ADD_SUBDIRECTORY(${CHILD})
  ENDIF()
ENDFOREACH()
